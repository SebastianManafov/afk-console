diff --git a/MinecraftClient/Proxy/ProxyHandler.cs b/MinecraftClient/Proxy/ProxyHandler.cs
index 5b1f6a1..a14f779 100644
--- a/MinecraftClient/Proxy/ProxyHandler.cs
+++ b/MinecraftClient/Proxy/ProxyHandler.cs
@@ -91,7 +91,8 @@ namespace MinecraftClient.Proxy
 
                     return proxy.CreateConnection(host, port);
                 }
-                else return new TcpClient(host, port);
+                else throw new System.InvalidOperationException(
+                    "Proxy-only policy: direct TCP connections are disabled.");
             }
             catch (ProxyException e)
             {
diff --git a/MinecraftClient/Protocol/MicrosoftAuthentication.cs b/MinecraftClient/Protocol/MicrosoftAuthentication.cs
index 6e719b1..4e524c1 100644
--- a/MinecraftClient/Protocol/MicrosoftAuthentication.cs
+++ b/MinecraftClient/Protocol/MicrosoftAuthentication.cs
@@ -210,5 +210,11 @@ namespace MinecraftClient.Protocol
         public static void OpenBrowser(string link)
         {
+            // Railway is intentionally headless. The device link and code are already
+            // printed to the secured dashboard, so never attempt to launch xdg-open.
+            if (Environment.GetEnvironmentVariable("MCC_HEADLESS") == "1")
+            {
+                return;
+            }
             try
             {
                 if (RuntimeInformation.IsOSPlatform(OSPlatform.Windows))
